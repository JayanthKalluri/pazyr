1. Complete the llm.py content read the prompt_template 

Create a CustomAgent class which supports 
- async call to llm_agent
- also create a method for creating the prompt using prompt_template.

- in models create the schemas for input and output.
- send this data to postgres.
